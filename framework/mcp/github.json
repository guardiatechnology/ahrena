{
  "_comment": "Ahrena MCP: GitHub server config. HTTP-first per lex-mcp §5 (transport preference: HTTP > binary > npx). Both platforms use the official remote endpoint at api.githubcopilot.com/mcp/, so no local Node/binary is required. Merged into platform MCP files during install. Do not edit — override via .ahrena/mcp/github.json instead.",
  "cursor": {
    "url": "https://api.githubcopilot.com/mcp/",
    "headers": {
      "Authorization": "Bearer ${env:GITHUB_PAT}"
    },
    "requires": []
  },
  "claude-code": {
    "type": "http",
    "url": "https://api.githubcopilot.com/mcp/",
    "headers": {
      "Authorization": "Bearer ${GITHUB_PAT}"
    },
    "requires": []
  }
}
