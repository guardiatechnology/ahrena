# Tech Task

Use this template for well-defined small tasks: chore, refactoring, maintenance, documentation fix, CI change, or any work that does not justify the depth of a feature request, epic, or user story.

## Why

State the motivation. What problem does this solve, what gap does it close, what risk does it remove? Connect to a real concern — avoid generic justifications.

<!-- placeholder: e.g. The tech-task template is referenced by lex-issue-quality but does not actually exist as a GitHub Issue Form, blocking contributors from following the Lex. -->

## What

State the objective and scope. What changes? What stays out? Be specific enough that a reviewer can tell when the task is done.

<!-- placeholder: e.g. Add `tech-task.yml` under `.github/ISSUE_TEMPLATE/` with Why/What/How sections; do not change the existing templates. -->

## How

State the implementation approach or definition of done. List the concrete steps, files involved, or success criteria.

<!-- placeholder: e.g.
- Create `framework/templates/contributing_templates/tech-task.md`.
- Create `.github/ISSUE_TEMPLATE/tech-task.yml` with required textareas (Why, What, How) and `evolvability ♻️` as the default label.
- Verify the template appears in the GitHub issue creation UI.
-->

## Additional Context

Screenshots, links, related issues, or any other relevant context (optional).

---

> To track progress, follow this issue on GitHub.
